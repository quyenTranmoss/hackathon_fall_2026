import React from 'react';
export default function SwellTraits({ dashboardData, isOwnProfile, profile }) { return <><div className="page-intro"><h2>Traits</h2><p>{isOwnProfile ? 'Your profile' : `${profile.name.split(' ')[0]}'s profile`} is built from 63 evolving traits across 9 workplace dimensions.</p></div><div className="traits-grid">{dashboardData.workplaceDimensions.map(dimension => <details className="card trait-card dimension-card" key={dimension.name}><summary><div className="trait-card-top"><h3>{dimension.name}</h3><strong>{dimension.score}%</strong></div><div className="bar large"><i className={dimension.color} style={{ width: `${dimension.score}%` }} /></div><p>{dimension.description}</p></summary><div className="representative-traits"><span>Representative traits</span>{dimension.traits.map(trait => <em key={trait}>{trait}</em>)}</div></details>)}</div></>; }
